# lib/

Server-side logic, not UI: the Meta/Instagram API client, Prisma database access, auth helpers, the BullMQ queue client, statistics aggregation, and the operational-alerts email logic. Imported by `app/api/` routes and by `worker/`.
