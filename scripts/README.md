# scripts/

One-off developer scripts, not part of the running app. `seed-demo-screenshot.ts` seeds a local-only demo workspace for taking screenshots or testing UI against fake data — never run it against a real `DATABASE_URL`.
