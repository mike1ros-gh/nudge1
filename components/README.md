# components/

Shared React components used across the dashboard pages under `app/(dashboard)/` — stat cards, charts, navigation, account selectors, and form inputs. UI only; data fetching and business logic live in `lib/`.
